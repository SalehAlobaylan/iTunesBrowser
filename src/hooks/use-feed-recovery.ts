import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';
import {approveFeedRecoveryPlan,createFeedRecoveryPlan,getFeedRecoveryPlan,issueFeedRecoveryReauth,listFeedRecoveryRuns} from '@/lib/api/cms/feed-recovery';
import {toast} from '@/components/ui/toast';
export const feedRecoveryKeys={all:['feed-recovery'] as const,runs:()=>['feed-recovery','runs'] as const,plan:(id?:string)=>['feed-recovery','plan',id] as const};
export function useFeedRecoveryRuns(){return useQuery({queryKey:feedRecoveryKeys.runs(),queryFn:listFeedRecoveryRuns,staleTime:10_000,refetchInterval:20_000});}
export function useFeedRecoveryPlan(id?:string){return useQuery({queryKey:feedRecoveryKeys.plan(id),queryFn:()=>getFeedRecoveryPlan(id!),enabled:Boolean(id)});}
export function useCreateFeedRecoveryPlan(){const qc=useQueryClient();return useMutation({mutationFn:createFeedRecoveryPlan,onSuccess:()=>{qc.invalidateQueries({queryKey:feedRecoveryKeys.all});toast({title:'Recovery preflight prepared',description:'No feed mutation is enabled in this foundation slice.',variant:'success'});},onError:(e:Error)=>toast({title:'Recovery preflight failed',description:e.message,variant:'destructive'})});}
export function useApproveFeedRecoveryPlan(){const qc=useQueryClient();return useMutation({mutationFn:async ({plan,password,phrase}:{plan:Awaited<ReturnType<typeof getFeedRecoveryPlan>>;password:string;phrase:string})=>{const proof=await issueFeedRecoveryReauth(password,plan);return approveFeedRecoveryPlan(plan.id,phrase,proof.proof);},onSuccess:()=>{qc.invalidateQueries({queryKey:feedRecoveryKeys.all});toast({title:'Recovery approval recorded',description:'The persisted cancel window is active; execution is not installed yet.',variant:'success'});},onError:(e:Error)=>toast({title:'Recovery approval failed',description:e.message,variant:'destructive'})});}
