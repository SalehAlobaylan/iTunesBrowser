'use client';
import {useState} from 'react';
import {Button} from '@/components/ui/button'; import {Card,CardContent,CardHeader,CardTitle} from '@/components/ui/card'; import {Input} from '@/components/ui/input'; import {Badge} from '@/components/ui/badge';
import {useApproveFeedRecoveryPlan,useCreateFeedRecoveryPlan,useFeedRecoveryPlan,useFeedRecoveryRuns} from '@/hooks/use-feed-recovery';
import type {FeedRecoveryLane,FeedRecoveryLevel} from '@/types/platform/feed-recovery';
export default function FeedRecoveryPage(){const [lane,setLane]=useState<FeedRecoveryLane>('news');const [level,setLevel]=useState<FeedRecoveryLevel>('repair');const [planID,setPlanID]=useState('');const [password,setPassword]=useState('');const [phrase,setPhrase]=useState('');const create=useCreateFeedRecoveryPlan();const plan=useFeedRecoveryPlan(planID);const approve=useApproveFeedRecoveryPlan();const runs=useFeedRecoveryRuns();const current=plan.data;
return <main className="space-y-6 p-4 md:p-8"><div><h1 className="text-2xl font-semibold">Feed Recovery</h1><p className="text-sm text-muted-foreground">Prepare bounded recovery evidence first. This foundation cannot alter feed content.</p></div><Card><CardHeader><CardTitle>Preflight</CardTitle></CardHeader><CardContent className="space-y-3"><div className="flex flex-wrap gap-2">{(['news','media','both'] as FeedRecoveryLane[]).map(x=><Button key={x} variant={lane===x?'default':'outline'} onClick={()=>setLane(x)}>{x}</Button>)}{(['repair','rotate','purge_reseed'] as FeedRecoveryLevel[]).map(x=><Button key={x} variant={level===x?'secondary':'outline'} onClick={()=>setLevel(x)}>{x.replace('_',' ')}</Button>)}</div><Button disabled={create.isPending} onClick={async()=>{const p=await create.mutateAsync({lane,level,capacity_mode:'safe_cutover'});setPlanID(p.id)}}>Prepare recovery plan</Button></CardContent></Card>{current&&<Card><CardHeader><CardTitle>Plan <Badge>{current.state}</Badge></CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><p>Sources: {current.source_count} · proof {current.source_checksum.slice(0,12)}…</p><p>Manifest: {current.manifest_hash}</p><p>Confirmation: <code>APPROVE FEED RECOVERY {current.manifest_hash.slice(0,12).toUpperCase()}</code></p><Input type="password" placeholder="Admin password for fresh re-auth" value={password} onChange={e=>setPassword(e.target.value)}/><Input placeholder="Confirmation phrase" value={phrase} onChange={e=>setPhrase(e.target.value)}/><Button disabled={approve.isPending||!password||!phrase} onClick={()=>approve.mutate({plan:current,password,phrase})}>Approve (no execution yet)</Button></CardContent></Card>}<Card><CardHeader><CardTitle>Recovery ledger</CardTitle></CardHeader><CardContent>{runs.data?.data?.length? <ul className="space-y-2">{runs.data.data.map(run=><li key={run.id}><Badge variant="secondary">{run.phase}</Badge> {run.lane} · {new Date(run.created_at).toLocaleString()}</li>)}</ul>:<p className="text-sm text-muted-foreground">No recovery runs recorded.</p>}</CardContent></Card></main>}
